// Ici on implémente les fonctionnalitées de traitement des données
// je commence l'implementation demain (classe ou fonction ?????????)
export function estimate(data: any): any {
  return console.log(data);
}
